import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10
  },
  image: {
    width: '100%',
    height: 300,
    resizeMode: 'center'
  }
});

export default styles;